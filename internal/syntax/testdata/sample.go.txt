package main

import "fmt"

// Comment with TODO in it.
const Greeting = "hello\tworld\n"

type Thing struct {
	Name string
	N    int
}

func main() {
	x := 0x1f
	y := 3.14
	var s = `raw
string`
	fmt.Printf("%d %s %v\n", x, s, y)
	if x > 1 && y < 2.0 {
		fmt.Println('c', true, nil)
	}
}
